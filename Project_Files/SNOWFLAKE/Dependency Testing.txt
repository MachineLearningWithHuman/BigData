#Check number of rows is equal or not
SELECT CASE
    when count(t1.*)=count(t2.*) then 'TRUE'
    else 'FALSE'
END AS RESULT
FROM "PC_FIVETRAN_DB"."FIVETRAN_S3"."CAMPAIGN" as t1, "DEVELOPMENT"."DEPENDENCY"."CAMPAIGN" as t2;
//Return True

#Check number of columns is equal or not
select case
when max(No_of_columns)-min(No_of_columns)=0 then 'True'
else 'False'
end as Result
from
(
  Select count(c1.column_name) as No_of_columns
from "DEVELOPMENT"."INFORMATION_SCHEMA"."COLUMNS" as c1 where c1.table_name='CAMPAIGN' 
UNION 
select count(c2.column_name) as cnt2 from "PC_FIVETRAN_DB"."INFORMATION_SCHEMA"."COLUMNS" as c2 
where c2.table_name='CAMPAIGN'
);
#Returns false becuase we drop 4 columns which was automatically generated by fivetran while loading



#Check number of rows is equal or not
SELECT CASE
    when count(t1.*)=count(t2.*) then 'TRUE'
    else 'FALSE'
END AS RESULT
FROM "PC_FIVETRAN_DB"."FIVETRAN_S3"."VISUALFORCEACCESSMETRICS" as t1, "DEVELOPMENT"."DEPENDENCY"."VISUALFORCEACCESSMETRICS" as t2;

#Check number of columns is equal or not
select case
when max(No_of_columns)-min(No_of_columns)=0 then 'True'
else 'False'
end as Result
from
(
  Select count(c1.column_name) as No_of_columns
from "DEVELOPMENT"."INFORMATION_SCHEMA"."COLUMNS" as c1 where c1.table_name='VISUALFORCEACCESSMETRICS' 
UNION 
select count(c2.column_name) as cnt2 from "PC_FIVETRAN_DB"."INFORMATION_SCHEMA"."COLUMNS" as c2 
where c2.table_name='VISUALFORCEACCESSMETRICS'
);
//Returns false becuase we drop 4 columns which was automatically generated by fivetran while loading


#Check number of rows is equal or not
SELECT CASE
    when count(t1.*)=count(t2.*) then 'TRUE'
    else 'FALSE'
END AS RESULT
FROM "PC_FIVETRAN_DB"."FIVETRAN_S3"."ZONE" as t1, "DEVELOPMENT"."DEPENDENCY"."ZONE" as t2;


#Check number of columns is equal or not
select case
when max(No_of_columns)-min(No_of_columns)=0 then 'True'
else 'False'
end as Result
from
(
  Select count(c1.column_name) as No_of_columns
from "DEVELOPMENT"."INFORMATION_SCHEMA"."COLUMNS" as c1 where c1.table_name='ZONE' 
UNION 
select count(c2.column_name) as cnt2 from "PC_FIVETRAN_DB"."INFORMATION_SCHEMA"."COLUMNS" as c2 
where c2.table_name='ZONE'
);
//Returns false becuase we drop 4 columns which was automatically generated by fivetran while loading